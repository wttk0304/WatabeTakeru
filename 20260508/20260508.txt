今後のGit管理について

今後featureブランチを作成などは書かないので、自身で作業単位でfeatureブランチを
作成し、Git-Flowベースで作業を行ってください。

作業途中でコンパイルできない状態でdevelopブランチにはマージしないでください。
developブランチは必ずとりあえず動いている状態を保ってください。

授業の作業が全て終わったタイミングでその日の日付でリリースブランチを作成し、
masterブランチへマージしてください。（例: release/20260501）